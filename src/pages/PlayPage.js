// import PropTypes from 'prop-types'
import styled from 'styled-components/macro'

PlayPage.propTypes = {}

export default function PlayPage(props) {
  return <Grid>PlayPage</Grid>
}

const Grid = styled.section`
  display: grid;
`
